# Server Configuration
PORT=3000
NODE_ENV=production

# Security & Secrets
# Generate a secure random string for JWT signing
JWT_SECRET=nsqtech_super_secret_jwt_key_2024_enterprise_grade
JWT_EXPIRES_IN=8h

# CORS Configuration
# Comma-separated list of allowed origins. Use '*' to allow all, or specify frontend deployment domain.
ALLOWED_ORIGINS=http://localhost:4200,https://nsqtech-dashboard.vercel.app
